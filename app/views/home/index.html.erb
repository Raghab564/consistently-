<div class="min-h-screen bg-gradient-to-br from-indigo-100 to-purple-200 flex items-center justify-center p-4">
  <div class="bg-white rounded-2xl shadow-xl p-8 max-w-xl text-center">
    <h1 class="text-4xl font-extrabold text-indigo-700 mb-4">Welcome to Consistently</h1>
    <p class="text-gray-700 text-lg mb-6">
      Build better habits and track your consistency. Stay motivated with streaks and keep yourself accountable every day.
    </p>

    <div class="flex justify-center space-x-4 mb-6">
      <% if user_signed_in? %>
        <%= link_to 'Go to Dashboard', authenticated_root_path, class: 'px-6 py-2 rounded-full bg-indigo-600 text-white hover:bg-indigo-700 transition' %>
      <% else %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'px-6 py-2 rounded-full bg-green-500 text-white hover:bg-green-600 transition' %>
        <%= link_to 'Log In', new_user_session_path, class: 'px-6 py-2 rounded-full bg-indigo-500 text-white hover:bg-indigo-600 transition' %>
      <% end %>
    </div>

    <img src="https://source.unsplash.com/400x200/?habit,calendar" alt="Habits" class="rounded-lg shadow-lg mx-auto">
  </div>
</div>
